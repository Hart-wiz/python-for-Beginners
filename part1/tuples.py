# Tuples__________________
# immutable (they cannot be changed after creation)


person = ("favor", 45, " engineer")

print(person[0])



#  converts from tuple to list
converted_to_list = list(person)


# converts from list to tuples
converted_to_tuple = tuple(list)